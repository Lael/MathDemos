export enum Duality {
    INNER = "Inner",
    OUTER = "Outer",
}

export enum Generator {
    LENGTH = "Length",
    AREA = "Area",
}

export enum Geometry {
    AFFINE = "Affine",
    HYPERBOLIC = "Hyperbolic",
    SPHERICAL = "Spherical",
}